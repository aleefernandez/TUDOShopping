import React from "react";
import CartWidget from "./CartWidget";

const NavBar = () => {
    return <div>
    Tienda
    Lista de productos
    
    <CartWidget/>
    </div>;
};

export default NavBar;